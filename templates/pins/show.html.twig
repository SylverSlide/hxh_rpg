{% extends 'base.html.twig' %}

{% block title %}Pin {{pin.id}}
{% endblock %}

{% block body %}
	{#<h1>{{ pluralize(pins|length, 'Pin') }}</h1>#}

	<article>
		<h1>{{ pin.title }}</h1>
		<p>
			{{ pin.description }}
		</p>
	</article>


	<nav>
		<ul>
			<li><a href="{{ path('app_pins_edit' , { id: pin.id })}}">Edit</a></li>
			<li><a href="#" onClick="event.preventDefault(); confirm('Are you sure') && document.getElementById('js-pin-delete-form').submit();">Delete</a></li>
			<li><a href="{{ path('app_home') }}">Go Back</a></>
			</ul>
			<form id="js-pin-delete-form" action="{{ path('app_pins_delete' , { id: pin.id }) }}" method="post" style="display:none">
			<input type="hidden" name="_method" value="DELETE">

			</form>
		</nav>
{% endblock %}
